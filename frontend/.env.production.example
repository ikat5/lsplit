# The frontend talks to the backend through this base URL (Vite inlines it at BUILD time).
# On Vercel, set VITE_API_BASE_URL as a Project Environment Variable instead of committing this file.
# Point it at your deployed Render backend, including the /api suffix and NO trailing slash:
VITE_API_BASE_URL=https://your-backend-name.onrender.com/api
