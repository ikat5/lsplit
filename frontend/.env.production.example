# The frontend talks to the backend through this base URL (Vite inlines it at BUILD time).
# On Vercel, set VITE_API_URL as a Project Environment Variable instead of committing this file.
# Backend origin only — no /api suffix (paths already include /api), no trailing slash:
VITE_API_URL=https://your-backend-name.onrender.com
